packages:
  # include packages in subfolders (change as required)
  - 'packages/**'
  - 'examples/**'
  - 'viewers/*'
  - 'website'
  # exclude generated deployment output
  - '!**/.vercel/**'
  # exclude the git submodule (and everything inside it)
  - '!packages/pdfium/pdfium-src/**'
